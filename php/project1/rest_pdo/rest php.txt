Building a REST API in PHP 8 leverages modern features like Attributes, Match expressions, and Union types to create cleaner, more performant web services. 

You can build one from scratch using core PHP or utilize specialized frameworks.Core PHP 8 Implementation StepsCreating an API without a framework provides full control over the codebase.

Environment Setup: Install PHP 8 and ensure extensions like pdo_mysql (for database access) and json are enabled.Routing & Request Handling: Use $_SERVER['REQUEST_METHOD'] to identify the HTTP method (GET, POST, PUT, DELETE).

PHP 8 Tip: Use Match Expressions for cleaner routing logic instead of lengthy switch statements.Database Connection: Use PHP Data Objects (PDO) for secure, consistent access to your database.


Handling JSON:Input: Read the raw input body using file_get_contents("php://input") and decode it with json_decode().

Output: Set the header to Content-Type: application/json and return data using json_encode().Response Codes: Use appropriate HTTP Status Codes (e.g., 200 for Success, 201 for Created, 404 for Not Found) to communicate the result of the request.

Modern PHP 8 Features for APIsAttributes: Use these for metadata handling, such as defining routes or validation rules directly above your functions.

Constructor Property Promotion: Reduces boilerplate code when initializing classes like Controllers or Repositories.Union Types: Allows methods to accept or return multiple types, improving type safety for dynamic API data.

Recommended PHP Frameworks for APIsIf you prefer a structured toolkit over building from scratch, these frameworks are optimized for REST:
	FrameworkBest ForNotable FeaturesLaravelFull-scale APIsBuilt-in authentication (Sanctum/Passport), elegant ORM (Eloquent), and powerful routing.Slim FrameworkMicro-servicesExtremely lightweight; ideal for simple APIs where you don't need a full-stack engine.SymfonyEnterprise-gradeHighly modular; uses the API Platform for rapid development.
	
	CodeIgniter 4PerformanceSmall footprint and fast execution, excellent for shared hosting environments.
	
	Security & Best PracticesAuthentication: Secure your endpoints using JWT (JSON Web Tokens) or OAuth 2.0 to ensure only authorized users access data.Input Validation: Always sanitize and validate user input to prevent SQL injection and XSS attacks.
	
	Statelessness: Ensure each request contains all the information needed to process it, without relying on server-side sessions.Testing: Use tools like Postman or Insomnia to manually test your endpoints during development.



============


https://medium.com/@jmrashed/building-a-production-ready-php-rest-api-framework-a-complete-guide-253c8ec95c40


Building a Production-Ready PHP REST API Framework: A Complete Guide
RASHED ZAMAN

Table of Contents
	Introduction
	Architecture Overview
	Core Framework Components
	Authentication & Security
	API Versioning Strategy
	Queue System Implementation
	Debug Bar & Development Tools
	CLI Support & Automation
	Database Management
	Testing & Quality Assurance
	Deployment & Production
	Best Practices & Patterns
	Performance Optimization
	Conclusion

Introduction

In the modern web development landscape, building robust, scalable REST APIs is crucial for any application. This article presents a comprehensive guide to building a production-ready PHP REST API framework from scratch, incorporating industry best practices, modern design patterns, and enterprise-grade features.

The PHP REST API Pro Kit is a lightweight yet powerful framework that demonstrates how to build a professional-grade API system without relying on heavy frameworks like Laravel or Symfony. This approach gives developers complete control over the codebase while maintaining simplicity and performance.

Why Build a Custom Framework?
Performance: No framework bloat, only what you need
Learning: Deep understanding of underlying concepts
Control: Complete customization capability
Simplicity: Clean, readable codebase
Scalability: Built for growth from day one
Architecture Overview
Project Structure
The framework follows a clean, modular architecture:

├── app/
│   ├── api/            # API versioning system
│   ├── cli/            # CLI commands and console
│   ├── config/         # Configuration files
│   ├── controllers/    # Request handlers (v1, v2)
│   ├── core/          # Core framework classes
│   ├── database/      # Migrations and seeders
│   ├── debugbar/      # Debug bar system
│   ├── exceptions/    # Exception handlers
│   ├── helpers/       # Utility classes
│   ├── middleware/    # Request middleware
│   ├── models/        # Data models
│   ├── queue/         # Queue system
│   ├── routes/        # Route definitions
│   ├── services/      # Business logic
│   └── tests/         # Test files
├── console            # CLI entry point
├── bootstrap/         # Application bootstrap
├── public/           # Web server document root
└── storage/          # Logs, cache, uploads

Design Principles
	Single Responsibility: Each class has one clear purpose
	Dependency Injection: Loose coupling between components
	Interface Segregation: Small, focused interfaces
	Open/Closed: Open for extension, closed for modification
	DRY (Don’t Repeat Yourself): Reusable components
	Core Framework Components
	Application Bootstrap
	The application bootstrap (bootstrap/app.php) initializes all core components:

<?php
// Load dependencies
require_once __DIR__ . '/../vendor/autoload.php';
require_once __DIR__ . '/../app/core/Autoloader.php';
// Initialize core components
$autoloader = new Autoloader();
$autoloader->register();
// Load environment configuration
Env::load(dirname(__DIR__) . '/.env');
// Initialize debug bar if enabled
if (Env::isDebugBarEnabled()) {
    $debugBar = DebugBar::getInstance();
    // Configure collectors...
}
// Create and return application instance
$app = new Application();
return $app;
Router System
The router handles URL routing with middleware support:

class Router
{
    private $routes = [];
    private $middleware = [];
    private $routeGroups = [];
    public function addRoute($method, $uri, $handler, $middleware = [])
    {
        // Route registration logic
    }
    public function group(string $prefix, array $middleware, callable $callback)
    {
        // Route grouping with middleware
    }
    public function dispatch(Request $request)
    {
        // Route matching and execution
    }
}
Request/Response Abstraction
Clean HTTP abstraction layer:

class Request
{
    public function getMethod(): string
    public function getUri(): string
    public function getHeaders(): array
    public function getJsonBody(): array
    public function getApiVersion(): ?string
}
class Response
{
    public static function json($data, $statusCode = 200)
    public static function success($message, $data = [], $statusCode = 200)
    public static function error($message, $statusCode = 500, $errors = [])
}
Database Layer
PDO wrapper with query tracking:

class Database
{
    private static $instance = null;
    private $connection;
    public static function getInstance()
    {
        if (self::$instance === null) {
            self::$instance = new Database();
        }
        return self::$instance;
    }
    public function getConnection()
    {
        return $this->connection;
    }
}

Authentication & Security
JWT Authentication
Secure token-based authentication system:

class JwtHelper
{
    public static function generateToken(array $payload): string
    {
        $header = base64url_encode(json_encode(['typ' => 'JWT', 'alg' => 'HS256']));
        $payload = base64url_encode(json_encode($payload));
        $signature = hash_hmac('sha256', $header . '.' . $payload, self::$secretKey, true);
        $signature = base64url_encode($signature);
        
        return $header . '.' . $payload . '.' . $signature;
    }
    public static function validateToken(string $token): ?array
    {
        // Token validation logic
    }
}

Security Middleware
Multiple layers of security protection:

class AuthMiddleware
{
    public function handle(Request $request, callable $next)
    {
        $token = $this->extractToken($request);
        if (!$token || !JwtHelper::validateToken($token)) {
            return Response::error('Unauthorized', 401);
        }
        return $next($request);
    }
}
class RateLimitMiddleware
{
    public function handle(Request $request, callable $next)
    {
        if ($this->isRateLimited($request)) {
            return Response::error('Too Many Requests', 429);
        }
        return $next($request);
    }
}

Security Features
Password Hashing: bcrypt with salt
SQL Injection Protection: Prepared statements
XSS Prevention: Input sanitization
CORS Handling: Configurable CORS middleware
Rate Limiting: IP-based request throttling
Input Validation: Comprehensive validation system
API Versioning Strategy
Version Detection Methods
The framework supports multiple version detection approaches:

URI Path Versioning (Recommended)
GET /api/v1/users GET /api/v2/users
Header-Based Versioning
X-API-Version: v2 Accept: application/vnd.api+json;version=2
Version-Specific Controllers
Each version has dedicated controllers with different response formats:


V1 Controller (Standard Format):

class UserController extends Controller
{
    public function index(Request $request)
    {
        $users = $this->userService->getAllUsers();
        return Response::json([
            'status' => 'success',
            'data' => $users,
            'version' => 'v1'
        ]);
    }
}

V2 Controller (Enhanced Format):

class UserController extends Controller
{
    public function index(Request $request)
    {
        $users = $this->userService->getAllUsers();
        return Response::json([
            'success' => true,
            'data' => $users,
            'meta' => [
                'version' => 'v2',
                'timestamp' => date('c'),
                'pagination' => ['page' => 1, 'per_page' => 10]
            ]
        ]);
    }
}
Backward Compatibility
Legacy endpoints remain functional while new versions provide enhanced features:

Legacy routes default to V1 behavior
Gradual migration path for existing clients
Clear deprecation strategy for old versions
Queue System Implementation
Queue Architecture
The queue system supports multiple drivers and job types:

interface JobInterface
{
    public function handle(): bool;
    public function failed(\Exception $exception): void;
    public function getMaxRetries(): int;
    public function getDelay(): int;
}
class QueueManager
{
    private static $instance = null;
    private $driver;
    public function push(JobInterface $job, string $queue = 'default'): string
    public function pop(string $queue = 'default'): ?array
    public function retry(string $jobId, JobInterface $job, string $queue): void
    public function failed(string $jobId, JobInterface $job, \Exception $exception): void
}
Built-in Job Types
Email Processing:

class SendEmailJob extends BaseJob
{
    private $to, $subject, $message;
    public function handle(): bool
    {
        return mail($this->to, $this->subject, $this->message);
    }
    public function failed(\Exception $exception): void
    {
        error_log("Email failed: " . $exception->getMessage());
    }
}
File Processing:

class ProcessFileJob extends BaseJob
{
    private $filePath, $operation, $options;
    public function handle(): bool
    {
        switch ($this->operation) {
            case 'resize': return $this->resizeImage();
            case 'compress': return $this->compressFile();
            case 'convert': return $this->convertFile();
        }
    }
}
Queue Drivers
Database Driver:

Uses MySQL for job storage
Automatic table creation
Transaction support
No external dependencies
Redis Driver:

High performance
Atomic operations
Delayed job support
Requires Redis extension
Usage Examples
// Simple email queuing
queue_email('user@example.com', 'Welcome!', 'Welcome message');
// File processing
queue_file_processing('/path/image.jpg', 'resize', ['width' => 800]);

// Custom job dispatch
$job = new CustomJob($data);
dispatch($job, 'custom_queue');
Debug Bar & Development Tools
Debug Bar Features
The integrated debug bar provides comprehensive development insights:

Performance Monitoring
Execution time tracking
Memory usage analysis
Peak memory consumption
2. Database Query Tracking

All SQL queries with timing
Parameter binding information
Query optimization hints
3. Debug Message Logging

Categorized logging (info, warning, error)
Timestamped entries
Context-aware messages
4. Request Information

HTTP method and URI
Headers and parameters
Request lifecycle tracking
Debug Bar Implementation
class DebugBar
{
    private static $instance = null;
    private $collectors = [];
    private $enabled = false;
    public function addCollector(CollectorInterface $collector): void
    {
        $this->collectors[$collector->getName()] = $collector;
    }
    public function collect(): array
    {
        $data = [];
        foreach ($this->collectors as $name => $collector) {
            $data[$name] = $collector->collect();
        }
        return $data;
    }
}
Security & Performance
Automatically disabled in production
IP whitelist support
Zero performance impact when disabled
No sensitive data exposure
CLI Support & Automation
Command System
The CLI system provides powerful development tools:

interface CommandInterface
{
    public function getName(): string;
    public function getDescription(): string;
    public function execute(array $args): int;
}
class Console
{
    private $commands = [];
    public function register(CommandInterface $command): void
    {
        $this->commands[$command->getName()] = $command;
    }
    public function run(array $argv): int
    {
        // Command execution logic
    }
}
Available Commands
Development Server
# cli commands 
php console serve [host] [port]
2. Database Management

# cli commands
php console migrate [fresh|rollback|seed]
3. Code Generation

# cli commands
php console make controller UserController php console make model User
4. Queue Management

# cli commands
php console queue work emails php console queue status default
5. Testing

# cli commands
php console test [specific-test-file]
6. Cache Management

# cli commands
php console cache clear
Command Implementation Example
class ServeCommand implements CommandInterface
{
    public function execute(array $args): int
    {
        $host = $args[0] ?? 'localhost';
        $port = $args[1] ?? '8000';
        
        echo "Starting server at http://{$host}:{$port}\n";
        passthru("php -S {$host}:{$port} -t public");
        
        return 0;
    }
}
Database Management
Migration System
Database migrations provide version control for database schema:

class Migrator
{
    public function migrate(): void
    {
        $migrations = $this->getPendingMigrations();
        foreach ($migrations as $migration) {
            $this->runMigration($migration);
        }
    }
    public function rollback(): void
    {
        $lastMigration = $this->getLastMigration();
        $this->rollbackMigration($lastMigration);
    }
}
Model Layer
Active Record pattern implementation:

abstract class Model
{
    protected $table;
    protected $fillable = [];
    protected $primaryKey = 'id';

    public function save(): bool
    {
        if ($this->exists()) {
            return $this->update();
        }
        return $this->insert();
    }

    public static function find($id)
    {
        // Find by primary key
    }
    public static function where($column, $operator, $value)
    {
        // Query builder methods
    }
}
Database Schema
The HRMS system includes comprehensive tables:

users — Employee accounts with roles
departments — Company departments
employee_profiles — Extended employee information
attendance — Daily attendance tracking
leave_types — Leave categories
leave_requests — Leave applications
payroll — Monthly salary processing
performance_reviews — Employee evaluations
tokens — JWT authentication tokens
Testing & Quality Assurance
Testing Strategy
Comprehensive testing approach:

Unit Tests — Individual component testing
Integration Tests — Component interaction testing
API Tests — Endpoint functionality testing
Performance Tests — Load and stress testing
PHPUnit Integration
class UserTest extends TestCase
{
    public function testUserCreation()
    {
        $user = new User(['name' => 'John', 'email' => 'john@example.com']);
        $this->assertTrue($user->save());
        $this->assertDatabaseHas('users', ['email' => 'john@example.com']);
    }
    public function testApiEndpoint()
    {
        $response = $this->get('/api/v1/users');
        $this->assertEquals(200, $response->getStatusCode());
        $this->assertJson($response->getContent());
    }
}
Quality Metrics
Code Coverage: Aim for 80%+ coverage
Performance: Response times under 200ms
Security: Regular vulnerability scanning
Documentation: Comprehensive API docs
Deployment & Production
Docker Support
Complete containerization setup:

FROM php:8.0-apache
COPY . /var/www/html/
RUN docker-php-ext-install pdo pdo_mysql
EXPOSE 80
# docker-compose.yml
version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:80"
    environment:
      - DB_HOST=mysql
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: secret
Environment Configuration
Production-ready environment setup:

# Production .env
APP_ENV=production
APP_DEBUG=false
DB_HOST=production-db-host
JWT_SECRET=secure-production-key
DEBUGBAR_ENABLED=false
QUEUE_DRIVER=redis
REDIS_HOST=redis-cluster
Performance Optimization
Caching Strategy
File-based caching for configuration
Redis for session storage
Database query result caching
2. Database Optimization

Proper indexing strategy
Query optimization
Connection pooling
3. HTTP Optimization

Gzip compression
HTTP/2 support
CDN integration
Best Practices & Patterns
Design Patterns Used
Singleton Pattern — Database connections, Queue manager
Factory Pattern — Response creation, Model instantiation
Observer Pattern — Event system, Middleware chain
Strategy Pattern — Queue drivers, Authentication methods
Decorator Pattern — Middleware wrapping
Code Quality Standards
PSR Standards — PSR-4 autoloading, PSR-12 coding style
SOLID Principles — Clean architecture implementation
Error Handling — Comprehensive exception management
Logging — Structured logging with context
Documentation — Inline documentation and API docs
Security Best Practices
Input Validation — All user input validated and sanitized
Output Encoding — Prevent XSS attacks
SQL Injection Prevention — Prepared statements only
Authentication — Secure JWT implementation
Authorization — Role-based access control
Rate Limiting — Prevent abuse and DoS attacks
Performance Optimization
Benchmarking Results
Performance metrics for the framework:

Response Time: Average 50ms for simple endpoints
Memory Usage: ~2MB base memory footprint
Throughput: 1000+ requests/second on standard hardware
Database Queries: Optimized with proper indexing
Optimization Techniques
Lazy Loading — Load components only when needed
Connection Pooling — Reuse database connections
Query Optimization — Efficient SQL queries with proper indexes
Caching Strategy — Multi-layer caching approach
Asset Optimization — Minified CSS/JS for debug bar
Scalability Considerations
Horizontal Scaling — Stateless design for load balancing
Database Sharding — Partition data across multiple databases
Queue Scaling — Multiple workers for background processing
Microservices Ready — Modular design for service extraction
Advanced Features
Event System
Implement event-driven architecture:

class EventDispatcher
{
    private $listeners = [];
    public function listen(string $event, callable $listener): void
    {
        $this->listeners[$event][] = $listener;
    }
    public function dispatch(string $event, $data = null): void
    {
        foreach ($this->listeners[$event] ?? [] as $listener) {
            $listener($data);
        }
    }
}
Caching Layer
Multi-level caching system:

interface CacheInterface
{
    public function get(string $key);
    public function set(string $key, $value, int $ttl = 3600): bool;
    public function delete(string $key): bool;
}
class FileCache implements CacheInterface
{
    public function get(string $key)
    {
        $file = $this->getCacheFile($key);
        if (!file_exists($file) || $this->isExpired($file)) {
            return null;
        }
        return unserialize(file_get_contents($file));
    }
}
API Documentation
Automated API documentation generation:

/**
 * @api {get} /api/v1/users Get Users
 * @apiName GetUsers
 * @apiGroup User
 * @apiVersion 1.0.0
 * 
 * @apiParam {Number} page Page number
 * @apiParam {Number} per_page Items per page
 * 
 * @apiSuccess {String} status Response status
 * @apiSuccess {Object[]} data User data array
 */
Monitoring & Logging
Logging System
Structured logging implementation:

class Logger
{
    public static function info(string $message, array $context = []): void
    {
        self::log('INFO', $message, $context);
    }
    public static function error(string $message, array $context = []): void
    {
        self::log('ERROR', $message, $context);
    }
    private static function log(string $level, string $message, array $context): void
    {
        $logEntry = [
            'timestamp' => date('Y-m-d H:i:s'),
            'level' => $level,
            'message' => $message,
            'context' => $context
        ];
        
        file_put_contents(
            STORAGE_PATH . '/logs/app.log',
            json_encode($logEntry) . "\n",
            FILE_APPEND | LOCK_EX
        );
    }
}
Health Monitoring
System health check endpoints:

class HealthController
{
    public function check(Request $request)
    {
        return Response::json([
            'api' => 'healthy',
            'timestamp' => date('Y-m-d H:i:s'),
            'version' => '1.0.0',
            'database' => $this->checkDatabase(),
            'memory_usage' => memory_get_usage(),
            'disk_space' => disk_free_space('.')
        ]);
    }
}
Future Enhancements
Roadmap
GraphQL Support — Alternative to REST endpoints
WebSocket Integration — Real-time communication
OAuth2 Server — Third-party authentication
Elasticsearch Integration — Advanced search capabilities
Microservices Architecture — Service decomposition
API Gateway — Centralized API management
Extension Points
The framework is designed for extensibility:

Custom Middleware — Add new middleware layers
Custom Drivers — Implement new queue/cache drivers
Custom Commands — Add CLI commands
Custom Collectors — Extend debug bar functionality
Custom Validators — Add validation rules
Conclusion
The PHP REST API Pro Kit demonstrates how to build a production-ready API framework that combines simplicity with power. Key achievements include:

Technical Excellence
Clean Architecture — Modular, maintainable codebase
Performance — Optimized for speed and efficiency
Security — Multiple layers of protection
Scalability — Built for growth and high traffic
Developer Experience — Comprehensive tooling and debugging
Business Value
Rapid Development — Quick API development and deployment
Cost Effective — No licensing fees, minimal infrastructure
Maintainable — Clear code structure and documentation
Flexible — Adaptable to various business requirements
Future Proof — Modern architecture and best practices
Learning Outcomes
Building this framework provides deep insights into:

HTTP Protocol — Understanding request/response cycles
Database Design — Efficient data modeling and querying
Security Principles — Authentication, authorization, and protection
Performance Optimization — Caching, indexing, and scaling
Software Architecture — Design patterns and best practices
DevOps Practices — Deployment, monitoring, and maintenance
Final Thoughts
This framework serves as both a practical tool and an educational resource. It demonstrates that building robust, scalable APIs doesn’t require heavy frameworks or complex architectures. With careful design, attention to detail, and adherence to best practices, a lightweight custom solution can outperform many existing frameworks while providing complete control and understanding of the underlying systems.

The journey of building this framework reinforces the importance of understanding fundamentals, following established patterns, and continuously improving through testing and iteration. Whether used in production or as a learning tool, this framework provides a solid foundation for modern web API development.

This article represents a comprehensive guide to building production-ready PHP REST APIs. The complete source code and documentation are available in the project repository, providing a practical reference for developers looking to implement similar solutions.

Github Repo: https://github.com/jmrashed/php-rest-api-pro-kit

3


PHP
Rest Api
No Framework
Best Practices
Api Development
3



RASHED ZAMAN

Follow
Written by RASHED ZAMAN
15 followers
·
18 following
Tech Lead with 10+ years in PHP, Laravel, Node.js & React. Expert in scalable SaaS, AI integration, and leading high-impact developer teams.

No responses yet
To respond to this story,
get the free Medium app.

Open in app
More from RASHED ZAMAN
Building a Robust Attendance System: Complete Guide to ZK BioMetric Device Integration with Node.js
RASHED ZAMAN
RASHED ZAMAN

Building a Robust Attendance System: Complete Guide to ZK BioMetric Device Integration with Node.js
In today’s digital workplace, attendance management has evolved from traditional punch cards to sophisticated biometric systems. ZKTeco…
Oct 3, 2025
Microservices Event-Driven Architecture with RabbitMQ and Docker Containers
RASHED ZAMAN
RASHED ZAMAN

Microservices Event-Driven Architecture with RabbitMQ and Docker Containers
In the fast-paced and ever-evolving world of software development, the need for scalable, resilient, and efficient architectures is…
Aug 8, 2023
Building a Secure & Scalable RESTful API in Go with MySQL, JWT, and Role-Based Access Control…
RASHED ZAMAN
RASHED ZAMAN

Building a Secure & Scalable RESTful API in Go with MySQL, JWT, and Role-Based Access Control…
A step-by-step guide to designing, developing, and deploying a production-ready REST API in Go with authentication, authorization, and…
Oct 6, 2025
Driver.js: Transforming User Onboarding with Seamless Product Tours
RASHED ZAMAN
RASHED ZAMAN

Driver.js: Transforming User Onboarding with Seamless Product Tours
In today’s digital landscape, user onboarding has become a critical factor in determining the success of software products. A seamless…
Dec 20, 2024
See all from RASHED ZAMAN
https://github.com/jmrashed/php-rest-api-pro-kit

===========
https://medium.com/@dharshithasrimal/php-rest-api-7441197312d7

PHP REST API
Dharshitha Senevirathne
Dharshitha Senevirathne

Follow
6 min read
·
Jun 28, 2024
300


3



Creating a REST API using PHP and MySQL provides a powerful way to interact with your database through simple HTTP requests. This guide will walk you through setting up a development environment using XAMPP, creating a MySQL database, building a PHP REST API, and testing it with Postman.


Step 1: Set Up XAMPP
Download and Install XAMPP
XAMPP is an open-source cross-platform web server solution stack package developed by Apache Friends, consisting mainly of the Apache HTTP Server, MariaDB database, and interpreters for scripts written in the PHP and Perl programming languages. You can download XAMPP from the official website. The installation process is straightforward — just follow the on-screen instructions.

You can follow this to install XAMPP

Start Apache and MySQL
Once XAMPP is installed, open the XAMPP Control Panel. Here, you will see various services that XAMPP can manage. Start the Apache and MySQL services by clicking the ‘Start’ button next to each. These services are necessary for running your PHP scripts and managing your MySQL database.

Step 2: Create a MySQL Database
phpMyAdmin
phpMyAdmin is a free software tool written in PHP, intended to handle the administration of MySQL over the Web. It can perform various tasks such as creating, modifying, or deleting databases, tables, fields, or rows, executing SQL statements, or managing users and permissions. You can access phpMyAdmin by navigating to http://localhost/phpmyadmin in your web browser.

Create a Database
Within phpMyAdmin, you can create a new database:

Click on the “New” link in the left sidebar.
Enter a name for your database (e.g., users).
Click “Create”.
Create a Table
With your new database selected:

Click on the “New” link to create a new table.
Name your table (e.g., users) and define the columns:
id (INT, Primary Key, Auto Increment)
name (VARCHAR(200))
email (VARCHAR(200))
Click “Save” to create the table.
Press enter or click to view image in full size

users table
Step 3: Create the PHP REST API
A REST API is a web service that uses HTTP requests to perform CRUD operations on resources. It allows different software applications to communicate with each other over the web. REST APIs are used because they are stateless, scalable, and follow standard protocols.

What is an API endpoint?
An API endpoint is a specific URL where an API (Application Programming Interface) can be accessed by a client application. It represents a distinct route to a particular function or data set provided by the API.

Create a Project Folder
Navigate to the htdocs folder in your XAMPP installation directory (e.g., C:\xampp\htdocs). This is the web root directory for your local Apache server. Create a new folder for your project (e.g., restapi). This folder will contain all the files for your REST API.

Create a Database Connection File (db.php)
This file will handle the connection to your MySQL database. Using PDO (PHP Data Objects) is a good practice because it provides a consistent interface for accessing databases and is less prone to SQL injection attacks.

<?php
$host = "localhost";
$user = "root";
$password = "";
$dbname = "users";

try {
    $pdo = new PDO("mysql:host=$host;dbname=$dbname", $user, $password);
    $pdo->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
} catch (PDOException $e) {
    die("Connection failed: " . $e->getMessage());
}
?>
Create the API File (api.php)
This file will contain the logic for handling different types of HTTP requests (GET, POST, PUT, DELETE). Depending on the request method, the corresponding function will be called to perform the necessary database operations.

<?php
header("Content-Type: application/json");
include 'db.php';

$method = $_SERVER['REQUEST_METHOD'];
$input = json_decode(file_get_contents('php://input'), true);

switch ($method) {
    case 'GET':
        handleGet($pdo);
        break;
    case 'POST':
        handlePost($pdo, $input);
        break;
    case 'PUT':
        handlePut($pdo, $input);
        break;
    case 'DELETE':
        handleDelete($pdo, $input);
        break;
    default:
        echo json_encode(['message' => 'Invalid request method']);
        break;
}

function handleGet($pdo) {
    $sql = "SELECT * FROM users";
    $stmt = $pdo->prepare($sql);
    $stmt->execute();
    $result = $stmt->fetchAll(PDO::FETCH_ASSOC);
    echo json_encode($result);
}

function handlePost($pdo, $input) {
    $sql = "INSERT INTO users (name, email) VALUES (:name, :email)";
    $stmt = $pdo->prepare($sql);
    $stmt->execute(['name' => $input['name'], 'email' => $input['email']]);
    echo json_encode(['message' => 'User created successfully']);
}

function handlePut($pdo, $input) {
    $sql = "UPDATE users SET name = :name, email = :email WHERE id = :id";
    $stmt = $pdo->prepare($sql);
    $stmt->execute(['name' => $input['name'], 'email' => $input['email'], 'id' => $input['id']]);
    echo json_encode(['message' => 'User updated successfully']);
}

function handleDelete($pdo, $input) {
    $sql = "DELETE FROM users WHERE id = :id";
    $stmt = $pdo->prepare($sql);
    $stmt->execute(['id' => $input['id']]);
    echo json_encode(['message' => 'User deleted successfully']);
}
?>
Step 4: Test the API Using Postman
Start Postman
Postman is a popular tool for testing APIs. It allows you to send different types of HTTP requests to your API and see the responses. You can download Postman from the official website.

Test GET Request
To test retrieving data:

Open Postman and create a new GET request.
Enter the URL: http://localhost/restapi/api.php.
Click “Send”. You should see a JSON response with all users in the database.
Press enter or click to view image in full size

GET request
Test POST Request
To test adding new data:

Create a new POST request.
Enter the URL: http://localhost/restapi/api.php.
Go to the “Body” tab, select “raw” and “JSON”.
Enter the JSON data and click “Send”. You should see a success message.
{
  "name": "John Doe",
  "email": "john@example.com"
}
Press enter or click to view image in full size

POST request
Test PUT Request
To test updating data:

Create a new PUT request.
Enter the URL: http://localhost/restapi/api.php.
Go to the “Body” tab, select “raw” and “JSON”.
Enter the JSON data and Click “Send”. You should see a success message.
{
  "id": 1,
  "name": "Jane Doe",
  "email": "jane@example.com"
}
Press enter or click to view image in full size

Test DELETE Request
To test deleting data:

Create a new DELETE request.
Enter the URL: http://localhost/restapi/api.php.
Go to the “Body” tab, select “raw” and “JSON”.
Enter the JSON data and click “Send”. You should see a success message.
{
  "id": 1
}
Press enter or click to view image in full size

DELETE Request
If you’re interested in getting the source code, you can download it from here!

Conclusion
This guide has walked you through the process of setting up a REST API using PHP and MySQL with XAMPP. You learned how to create a MySQL database, build a PHP script to handle RESTful requests, and test the API using Postman. With these skills, you can now develop and test robust APIs for your web applications.

Get Dharshitha Senevirathne’s stories in your inbox
Join Medium for free to get updates from this writer.

Enter your email
Subscribe

Remember me for faster sign in

Checkout my next article on Creating a PHP CRUD application with MySQL.

If you have any questions then feel free to reach out to me.

You can support me by buying me a coffee

I am a freelance software developer specializing in creating scalable software solutions and providing IT consulting services. Feel free to reach out to me at dharshithasrimal@gmail.com.




I don't understant this statement: $input = json_decode(file_get_contents('php://input'), true);
What does it mean? Where comes from the php://input?
Everything else is almost clear.....


* file_get_contents('php://input') : This reads the raw data sent in the HTTP request body.
If the client sends JSON data in the body (e.g., {"name": "John", "email": "john@example.com"}), this will retrieve it as a raw string: {"name": "John"…more
1

Precise and clear !
I come from the Java world... but with this post, I was able to get it up and running in no time, big thank you!


===================================================



A PHP REST API  is a web service that uses Representational State Transfer (REST) principles to allow different applications to communicate over HTTP. PHP is a popular choice for building these APIs due to its native support for HTTP methods, deep integration with databases like MySQL, and a vast ecosystem of frameworks. [1, 2, 3, 4]
Core REST Methods in PHP
REST APIs typically perform CRUD (Create, Read, Update, Delete) operations using standard HTTP verbs: [1, 2]
    • GET: Retrieves data from the server (e.g., fetching a list of users).
    • POST: Sends new data to the server to create a resource.
    • PUT / PATCH: Updates existing resources on the server.
    • DELETE: Removes a specific resource from the server. [1, 2, 3, 4, 5, 6]
Development Approaches
You can build a REST API in PHP using two primary methods: [1]
1. Using PHP Frameworks (Recommended)
Frameworks provide built-in tools for routing, security, and database management, which speeds up development and improves maintainability. [1, 2]
    • Laravel: The most popular full-stack framework with extensive API features like Laravel Sanctum for authentication.
    • Slim Framework: A micro-framework ideal for small to medium APIs that need to be fast and lightweight.
    • Symfony / API Platform: A powerful, enterprise-grade solution specifically designed for building high-performance REST and GraphQL APIs.
    • Lumen: A faster, lighter version of Laravel focused purely on microservices and APIs. [1, 2, 3, 4, 5]
2. Native PHP (From Scratch) [1, 2]
For simple projects or learning purposes, you can build an API without a framework by manually handling request headers and global variables like $_SERVER['REQUEST_METHOD']. [1, 2]
    • Headers: Use header("Content-Type: application/json") to ensure the client receives data in JSON format.
    • Database: Connect to MySQL using PDO (PHP Data Objects) for secure, prepared statements to prevent SQL injection.
    • Input: Read raw POST/PUT data using file_get_contents("php://input"). [1, 2, 3]
Essential Tools
    • XAMPP: Provides a local server environment (Apache, MySQL, PHP).
    • Postman: A popular tool for testing API endpoints by sending various HTTP requests and inspecting responses.
    • Composer: The dependency manager for PHP, used to install frameworks and libraries. [1, 2, 3, 4, 5]


===================


https://www.geeksforgeeks.org/php/building-a-rest-api-with-php-and-mysql/

Building a REST API with PHP and MySQL
Last Updated : 23 Jul, 2025
This brief tutorial is a step-by-step guide on how to develop a REST API using PHP and MySQL. REST API will implement HTTP commands (Get, Post, Put, DELETE) and response will be in form of JSON. For development setup, we will be using the XAMPP while for testing of the API, we will use the Postman app.

Steps to Build REST API with PHP and MySQL
Step 1: Download and Install XAMPP
Open the XAMPP official website for download.
xampp
Xampp Official Website
Download and set up XAMPP; open the XAMPP control panel.
xammpcontrol
Xampp Control Panel
That completes the installation process of PHP, MySQL, and Apache Server, now start the apache and mysql services.
Open a browser and navigate to http://localhost/dashboard. You should see the XAMPP dashboard.
Step 2: Create the Database
To overcome this, you simply have to type the following address in the address bar of your browser http://localhost/phpmyadmin/
A new database is created on clicking the “New” button available in the sidebar.
Type in the new name geeksforgeeks and then click on the “Create” button.
Gfg-db
Create geeksforgeeks database
Step 3: Create a Table
Select the geeksforgeeks database.
Click on the "SQL" tab and execute the following SQL code to create a users table
CREATE TABLE users (
id INT(6) UNSIGNED AUTO_INCREMENT PRIMARY KEY, 
name VARCHAR(30) NOT NULL,
email VARCHAR(50) NOT NULL,
age INT (3) NOT NULL);
db_name
Create users table
Step 4: Create the Project Folder
Navigate to the 'htdocs' directory in your XAMPP installation (usually 'C:\xampp\htdocs').
Set up a new folder that will be labeled geeksforgeeks_api.
xamppppppppp
Go to the location and add files
Step 5: Create a Database Connection File
Navigate to the folder geeksforgeeks_api and in that make a new file called db. php.
Add the following code to establish a connection to the MySQL database:

<?php
$servername = "localhost";
$username = "root";
$password = "";
$dbname = "geeksforgeeks";

// Create connection
$conn = new mysqli($servername, $username, $password, $dbname);

// Check connection
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}
?>
3. Create the API File
In the root directory of the geeksforgeeks_api, open another file and name it api. php.
Add the following code to handle various HTTP methods (GET, POST, PUT, DELETE):

<?php
include 'db.php';

header("Content-Type: application/json");

$method = $_SERVER['REQUEST_METHOD'];
$input = json_decode(file_get_contents('php://input'), true);

switch ($method) {
    case 'GET':
        if (isset($_GET['id'])) {
            $id = $_GET['id'];
            $result = $conn->query("SELECT * FROM users WHERE id=$id");
            $data = $result->fetch_assoc();
            echo json_encode($data);
        } else {
            $result = $conn->query("SELECT * FROM users");
            $users = [];
            while ($row = $result->fetch_assoc()) {
                $users[] = $row;
            }
            echo json_encode($users);
        }
        break;

    case 'POST':
        $name = $input['name'];
        $email = $input['email'];
        $age = $input['age'];
        $conn->query("INSERT INTO users (name, email, age) VALUES ('$name', '$email', $age)");
        echo json_encode(["message" => "User added successfully"]);
        break;

    case 'PUT':
        $id = $_GET['id'];
        $name = $input['name'];
        $email = $input['email'];
        $age = $input['age'];
        $conn->query("UPDATE users SET name='$name',
                     email='$email', age=$age WHERE id=$id");
        echo json_encode(["message" => "User updated successfully"]);
        break;

    case 'DELETE':
        $id = $_GET['id'];
        $conn->query("DELETE FROM users WHERE id=$id");
        echo json_encode(["message" => "User deleted successfully"]);
        break;

    default:
        echo json_encode(["message" => "Invalid request method"]);
        break;
}

$conn->close();
?>
After running the installation, it is advised to take the API for a test using the Postman tool.
Steps to Test the API using Postman
1. GET Request:
Open Postman.
Specify the request type to Get.
Enter http://localhost/geeksforgeeks_api/api.php at the end of the request URL.
Click "Send. "
Looking at the JSON response, you should be able to get all the users currently in the database.
Screenshots:
GET Request
get
GET REQUEST
2. POST Request:
Make the request type to POST instead of GET.
Enter http://localhost/geeksforgeeks_api/api.php in the request URL.
The “Body” tab can be selected, and under the “Data format”, choose “Raw” that can be “JSON”.
Add the following JSON data:
{
    "name": "GeeksforGeeks",
    "email": "geek@geeksforgeeks.com",
    "age": 28
}
Click "Send. "
Post this you should see a success message and the new user should be stored in the database.
post
POST REQUEST
POST_DATA-min
The data has been added into the darabase
GET Request after POST Request:
afterpost
GET Request after POST request
3. PUT Request:
Alters the request type to PUT.
Enter http://localhost/geeksforgeeks_api/api.php?id=1 in the request URL if the user with id=1 is exist.
In the "Body" tab, add the updated JSON data:
{
    "name": "Write for GeeksforGeeks",
    "email": "geeksforgeeks@geeksforgeeks.com",
    "age": 26
}
Click "Send. "
There should be a success message and after that, the user information will be modified in the database.
put
PUT request in Postman
GET Request after PUT Request:
4. DELETE Request:
Modify the request type to delete.
Enter http://localhost/geeksforgeeks_api/api.php?id=1 of the user with id = 1 added to the request URL (provided there is a user with such ID).
Click "Send. "
You should be seeing a success message and in the database, the user will be deleted.
delete
DELETE REQUEST
2. GET Request after DELETE Request:
afterdelete
GET REQUEST AFTER DELETE
Conclusion
But if you follow the measures that are pointed out in this guide, you can create a REST API yourself, using PHP and MySQL. This is the most flexible of the four methods as it gives the developer full control of the APIs and its instantiation to suit the project being developed. This implies that the API is tested with postman to ensure that all the functionalities are responding as required.
