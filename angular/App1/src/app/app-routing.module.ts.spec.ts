import { AppRoutingModuleTs } from './app-routing.module.ts';

describe('AppRoutingModuleTs', () => {
  it('should create an instance', () => {
    expect(new AppRoutingModuleTs()).toBeTruthy();
  });
});
