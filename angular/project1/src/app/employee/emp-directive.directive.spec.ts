import { EmpDirectiveDirective } from './emp-directive.directive';

describe('EmpDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new EmpDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
