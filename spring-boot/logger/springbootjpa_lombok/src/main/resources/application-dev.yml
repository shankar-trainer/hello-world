spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: password

server:
  port: 7070
#localhost:7070/swagger-ui.html