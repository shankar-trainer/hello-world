C:\test\employee-git

git init
git add * 
git commit -m "added"

add all files of git here 

as  default is \git\employee-service

so add in spring employee cofig client application.properties

spring.application.name=employee-service
