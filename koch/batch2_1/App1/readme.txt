maven life cycle methods 

  clean -- clean/ delete the old classes and target folder
  test  -- test using junit
  package -- whole project in the form of jar, war 
  
  install -- package and install it in ur local maven repository


   spring core maven dependency
   