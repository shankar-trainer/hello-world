// var buffer=new Buffer(230)

// buffer.write('we will learn php too')


// console.log(buffer.toString)

var buffer1=Buffer.alloc(245)
buffer1.write('we will learn php too')
console.log(buffer1.toString)

