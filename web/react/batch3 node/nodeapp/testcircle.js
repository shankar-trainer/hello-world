var c=require('./circle')
var radius=5.6;
console.log(`area of circle with radius ${radius} is ${c.area(4)}`)
console.log(`circumference of circle with radius ${radius} is ${c.circum(4)}`)

console.log(c.circum(5))
